import styled from 'styled-components'

const Table = styled.table`
`

const HeaderRow = styled.thead`
`

const HeaderCell = styled.th`
`

const Row = styled.tr`
`

const Cell = styled.td`
`

export { Table, HeaderRow, HeaderCell, Row, Cell }
