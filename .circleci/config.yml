version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-export:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install -g gh-pages
      - deploy: 
          command: if [ $CIRCLE_BRANCH = master ]; gh-pages --dist out; fi
workflows:
    build-and-export:
      jobs:
        - build-and-export