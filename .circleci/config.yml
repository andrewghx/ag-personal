version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-export:
    executor:
      name: node/default
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: echo hi
      - deploy: 
          command: if [ $CIRCLE_BRANCH = master ]; then git config user.email "andrewg010@gmail.com"; git config user.name "andrewghx"; fi
workflows:
    build-and-export:
      jobs:
        - build-and-export